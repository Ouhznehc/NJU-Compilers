int main()
{
    int n;
    n = read();
    write(n);
    if (n > 0) {
        write(2);
        write(n > 0);
        write(n < 0);
        write(1);
    }
    else if(n < 0) {
        write(3);
        write(n < 0);
        write(-1);
    }
    else write(0);
    return 0;
}
